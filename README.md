# in-progress landing

One-page landing for https://t.me/everybodycandoit

## Quick start
Open `index.html` in browser.

## Replace hero media
Put your video as `assets/hero.mp4` (or update `<source>` in `index.html`).
